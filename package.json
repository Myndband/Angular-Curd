{
  "name": "angular crud",
  "version": "1.0.0",
  "engines": {
      "node": ">=14 <19"
  },
  "scripts": {
      "start": "cd angular/built",
      "prebuild": "cd angular && npm run build",
      "build": "cd angular && npm run build"
  }
}